Backend:
    Enter backend and call npm install and then npm run dev

Frontend:
    Enter frontend and call npm install and then npm start

Can add listings and filter them based on certain fields.  Clicking on a listing will lead to a different page that shows the reviews for that listing.
